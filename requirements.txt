# Exact application and development dependencies. Update deliberately and run checks.
Django==5.2.17
asgiref==3.12.1
sqlparse==0.6.0
ruff==0.16.8
# Django requires timezone data on Windows.
tzdata==2026.2; sys_platform == "win32"
